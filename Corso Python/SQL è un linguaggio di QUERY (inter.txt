SQL è un linguaggio di QUERY (interrogazione query)
modificare e gestire dati
è un linguaggio di dominio specifico 

il modo più banale e semplice per richiamare un db è un SELECT 
SELECT = FROM world(nome db).city(nome tabella)

#use world: -> world in schemas diventa grassetto, significa che siamo già entrati nel database e basta fare
SELECT * FROM city;
query più semplice in assoluto perché richiama informazioni senza vincolo

ma noi molto probabilmente per data analisi non vogliamo vedere tutti i dati

SELECT Name FROM City:
ora ci da result 7, una tabella temporanea che ci da i risultati richiesti

SELECT Name, Population FROM city;
ci selezione il nome e la loro popolazione (di ogni città)

a sx tutta la struttura del database

SINTASSI SQL:
non è case sensitive, quindi minuscolo o maisucolo non fa differenza perché riconosce comunque le parole chiave

Un modo ordinato per scrivere query è una parola chiave per ogni riga
1. SELECT Name, Population (poosso mandare a capo anche population, MySQL non si confonde)
2. FROM city;

sono comunque parlanti gli errori di MySQL

le parole chiavi hanno un ordine SPECIFICO:
NO

1. FROM city
2. Select Name

KEYWORD DI MYSQL: UPDATE, DELETE, INSERT INTO, DATABASE, CREATE, ALTER (DB, TABLE), DROP (DB, TABLE)

SCHEMA RELAZIONALE:
I nostri DB devono contenere i dati ordinati e messi in relazione tra di loro
ogni tabella è identificata di un nome e quel nome identifica quel nome in una tabella

ogni tabella deve avere una chiave primaria

ci permette di mettere in relazione due tabelle insieme

con una chiave primaria può essere portata per relazionare una riga di tabella con un'altra
es. AFG di Afganistan la ritroviamo in country Language in country code (creo la relazione con country)

creazione di una tabella con le sue chiavi primarie

5 create table utente (
id int primary key,
nome varchar(50)
)

i costraint sono opzionali, posso inserire una colonna senza. 

per commentary una singola riga si può usare il cancelletto ma il vero commento è --
commento multiriga /* ad iniziio e */ alla fine

 
foreign key(idUtente) references utente(id)
chiave esterna 

ricordarsi il tipo di dato quando si crea una tabella

alter per modificare una tabella
3 tipi di mod
#add cap int
#drop + tabella
#modify colum ID int primary key devo riscrivere le info

il costraint è un VINCOLO specifico che assegniamo ad una colonna
ad es primary key ci da solo valori univoci che identificheranno i val di quella tabella

not null : no val vuoti
unique : inserire solo val univoci (come primary key)
primary key
foreign key
default : il val di default se l'utente non inserisce niente
check : forza il rispetto di regole (check >= 18)
auto_increment permette di aumentare automaticamente un valore

in analisi dati le date sono fondamentali per questo è importante il tipo di date

count tutto i valori
count (distinct .) solo quelli univoci

parola chiave WHERE filtro di colonna
from filtro di riga

sql