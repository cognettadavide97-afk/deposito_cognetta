n = int(input("inserisci un numero: "))
if n % 2 == 0:
    print("il numero è pari")
else: print("il numero è dispari")
#creo un ciclo ripetibile dall'utente in cui stampo tutti i numeri da n a 0
while True:
    n >= 0
    while loop == "si" and n >= 0:
        #utilizzo di range per creare una lista
        for i in range(n, -1, -1):
            print(i)
            #richiesta all'utente se vuole ripetere
            loop = input("vuoi ripetere: (si/no)")
    #creo una lista con i valori stampati precedentemente
    lista = [range]
        #devo stampare il quadrato di ogni numero nella lista
    #attraverso while e if, determinare un sistema che prende in input una lista di numeri che è stata precedentemente valorizzata dall'utente